wb = xlsx_package.workbook
wb.add_worksheet(name: "Produits") do |sheet|
  sheet.add_row ['Type', 'Légume', 'Famille']
  @products.each do |product|
    sheet.add_row [product.product_type, product.name, product.product_group.name]
  end
end
